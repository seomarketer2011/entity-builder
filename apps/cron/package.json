{
  "name": "@entity-builder/cron",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cron pinger: triggers one sync step on the web app every 2 minutes.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "echo 'trivial pinger — no tests'",
    "deploy:cf": "wrangler deploy"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "wrangler": "^4.110.0",
    "@cloudflare/workers-types": "^4.20260620.0"
  }
}
