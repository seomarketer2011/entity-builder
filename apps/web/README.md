# apps/web

Next.js dashboard + API. Implemented in **Phase 1** (see `docs/ROADMAP.md`
and `docs/UI_SPEC.md`). Intentionally empty in Phase 0 so the framework
version is chosen at implementation time, not frozen in the scaffold.
