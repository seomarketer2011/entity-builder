{
  "name": "entity-builder",
  "private": true,
  "version": "0.1.0",
  "description": "SEO Opportunity Engine — entity graph, page manifests, and explainable opportunity detection for service businesses (industry x service x location).",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.33.0"
}
